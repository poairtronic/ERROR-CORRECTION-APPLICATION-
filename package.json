{
  "name": "ecr-monolithic",
  "version": "1.0.0",
  "description": "Monolithic deployment for ECR System",
  "private": true,
  "workspaces": [
    "frontend",
    "ecr-system"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start:prod --workspace=ecr-system"
  }
}
